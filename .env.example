# Optional: OpenF1 API key to unlock LIVE timing during a running session.
# Between sessions the Live tab works without this. Get a key at https://openf1.org
# Copy this file to `.env` and fill in the value (Vite exposes VITE_* to the app).
VITE_OPENF1_KEY=
